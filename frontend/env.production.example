# URL de la API en producción
# El código detecta automáticamente el protocolo (HTTP/HTTPS) del navegador
# Puedes usar HTTPS aquí - el código lo ajustará a HTTP si el sitio está en HTTP
# Cuando configures SSL, funcionará automáticamente con HTTPS
REACT_APP_API_URL=https://sistema.vanguardschools.edu.pe/api

